; 旧版 FRP（INI）客户端：将下列段落追加到 frpc.ini（[common] 已存在则不要重复）。
; server_port 必须等于 frps 的 bind_port（你当前为 6000）。
; 云安全组放行 remote_port（6022）。Secret LOCAL_APK_SSH_PORT 已设为 6022。

[ssh_github_actions]
type = tcp
local_ip = 127.0.0.1
local_port = 22
remote_port = 6022
